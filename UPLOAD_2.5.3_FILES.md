# Upload v2.5.3

Upload every file and folder from this ZIP to the repository root, preserving paths. Replace existing files when prompted.

Changed/new files:
- `index.html`
- `tahoe.html`
- `service-worker.js`
- `VERSION.txt`
- `js/core/quest-core.js`
- `js/tahoe253.js`
- verification and changelog files

The character PNG files are not included because the deployed Adventure 1 Gold Master files are correct. The bug was entirely inside the shared Canvas renderer.
