# Upload v2.4.1

Upload every file and folder from this patch to the repository root, preserving paths. Replace `index.html` and `tahoe.html`.

After GitHub Pages deploys, open the site once in Safari/Chrome. The service worker will install. Future releases using a new service-worker VERSION will update automatically.
