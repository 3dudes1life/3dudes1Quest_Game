# Upload v2.5.1

Upload these files to the repository root, preserving folders:

- `tahoe.html` — replace
- `service-worker.js` — replace
- `js/pwa241.js` — replace
- `js/core/quest-core.js` — replace
- `js/tahoe251.js` — new

Do not delete `js/tahoe250.js`; it may remain as an old release file.

After GitHub Pages finishes deploying, refresh once. Installed Home Screen copies will detect the new service worker and reload automatically.
